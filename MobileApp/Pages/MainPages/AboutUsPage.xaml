<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MobileApp.Pages.AboutUsPage"
             Title="AboutUsPage">
    <Grid RowDefinitions="Auto, *"  
    RowSpacing="10"
  Margin="20">

        <Label 
            Text="About Us"
            Grid.Row="0"
            VerticalOptions="Center" 
            HorizontalOptions="Center" 
            FontSize="Large"/>
        <CollectionView Grid.Row="1"
               x:Name="CvAllWebsites"
                SelectionMode="Single"
                VerticalScrollBarVisibility="Always"
                VerticalOptions="FillAndExpand">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical"
                           ItemSpacing="30" />
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="10" CornerRadius="8" BackgroundColor="#F5F5F5">
                        <HorizontalStackLayout Spacing="10">
                            <Label Text="{Binding TitleName}" FontSize="Medium" />
                            <Button Text="View" Clicked="OnWebsiteClicked" CommandParameter="{Binding Id}" />
                        </HorizontalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>