@model DatabaseAPI.Models.General.Invoice


@{
    ViewData["Title"] = "Order Confirmed";
}

<div class="container">
    <p>The order confirmation</p>
</div>
<div class="container">
    <p>Your order was succesful</p>
    <p>Order details:</p>
    <p>Invoice ID: @Model.InvoiceId</p>
    @if (Model.CouponId != null)
    {
        <p>Ticket Sum: @Model.TicketSum PLN</p>
        <p>Coupon used: @Model.Coupon.Name </p>
        <p>Coupon Discount: @Model.CouponDiscount PLN</p>
    }
    <p>Sum: @Model.TotalSum PLN</p>
    <p>Payment Method: @Model.PaymentMethod.Name</p>
    <p>Payment status: @Model.PaymentStatus</p>
    <p>Order status: @Model.OrderStatus</p>

    <form asp-controller="Cart" asp-action="Index" method="post">
        <button type="submit" class="btn btn-outline-dark">Go to the cart</button>
    </form>
</div>