@using PortalWWW.Helpers
@using static PortalWWW.Helpers.GenericCheck;
@model DatabaseAPI.Models.CMS.Newsletter;

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>

<div>
    <h4>@Model.Name</h4>
    <hr />
    <dl class="row">        
        <form asp-action="Delete" asp-route-id="@Model.Id">

        <dt class="col-sm-2">
            @Html.DisplayNameFor(item => item.Name)
        </dt>
        <dd class="col-sm-10">
                @Html.DisplayFor(item => item.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(item => item.Description)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(item => item.Description)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(item => item.Content)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(item => item.Content)
        </dd>
        <dt class="col-sm-2">
                @Html.DisplayNameFor(item => item.Position)
        </dt>
        <dd class="col-sm-10">
                @Html.DisplayFor(item => item.Position)
        </dd>
        <div class="form-group">
            <input type="submit" value="Delete" class="btn btn-primary" />
        </div>
        </form>
    </dl>        
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>